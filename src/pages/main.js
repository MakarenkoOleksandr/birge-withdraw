export default function Tab() {
    return(
        <div className="buttons">
            <button onClick={binance}>Binance</button>
            <button onClick={okx}>OKX</button>
        </div>
    );
};
